export enum MoodLevel {
  GREAT = 'Ótimo',
  GOOD = 'Bem',
  NEUTRAL = 'Normal',
  BAD = 'Mal',
}

export enum StorageKeys {
  USER = '@h-bit:user',
  RECORDS = '@h-bit:records',
}

export enum Screen {
  HOME = 'Início',
  TRACK = 'Registrar',
  PROFILE = 'Perfil',
}